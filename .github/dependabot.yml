version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # A week's cooldown before a fresh action release is proposed.
    cooldown:
      default-days: 7
